# DuckTodo 环境变量示例文件（请复制为 .env 使用）
# 说明：
# - 本文件仅示例用途，部署时请使用 .env 并替换为真实生产值
# - .env 由 docker-compose.yml 读取并传入容器，容器会据此生成 Nginx 反代与后端配置
# - 切勿将包含真实密码/密钥的 .env 提交到远程仓库

# 后端 Spring Profile（容器默认 prod；不建议在生产改为 dev）
SPRING_PROFILES_ACTIVE=prod

# 宿主机对外端口映射（可根据需要修改）
# 浏览器访问前端页面的宿主机端口 → 容器内 Nginx 端口
HOST_HTTP_PORT=8080
# 外部直接访问后端 API 的宿主机端口 → 容器内后端端口
HOST_API_PORT=8081

# 数据库连接（外部 MySQL 服务；请替换为真实地址与凭据）
# 主机名或地址（例如 mysql、localhost、db.example.com）
MYSQL_URL=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=DuckTodo
MYSQL_USERNAME=ducktodo
MYSQL_PASSWORD=ducktodo_password

# 对象存储（外部 MinIO 服务；请替换为真实地址与凭据）
# endpoint 可为 http://minio-host:9000 或 https://minio.example.com
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=ducktodo
# 生产环境通常不在启动时自动创建桶，建议设为 false
MINIO_ENSURE_BUCKET=false

# JWT 签名密钥与过期时间（单位：秒）
# 请务必将 JWT_SECRET 更换为复杂随机字符串
JWT_SECRET=please_change_me
JWT_EXPIRE_SECONDS=86400
